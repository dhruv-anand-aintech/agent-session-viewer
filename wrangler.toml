# Remote access is now handled by ngrok via `npx agent-session-viewer --remote`.
# This wrangler config is kept for reference only — no deployment needed.
name = "agent-session-viewer"
main = "worker/index.ts"
compatibility_date = "2024-01-01"
